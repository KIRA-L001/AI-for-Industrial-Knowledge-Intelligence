# ─── Web framework ───
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# ─── Database ───
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10

# ─── Auth / security ───
passlib==1.7.4
bcrypt==4.0.1
pyjwt==2.10.1
email-validator==2.2.0

# ─── Async jobs ───
celery==5.4.0
redis==5.2.1

# ─── Object storage ───
minio==7.2.13

# ─── Knowledge graph ───
neo4j==5.27.0

# ─── Vector DB ───
qdrant-client==1.12.2

# ─── Document / OCR pipeline ───
pypdf==5.1.0
pdf2image==1.17.0
python-docx==1.1.2
openpyxl==3.1.5
pytesseract==0.3.13
Pillow==11.0.0

# ─── AI / RAG (provider-agnostic) ───
# NOTE: heavy ML models (torch, sentence-transformers, cross-encoder) live in
# requirements-ml.txt. Without them the app uses deterministic offline fallbacks
# (hashing embedder, lexical reranker, stub LLM). Install requirements-ml.txt to
# enable real Sentence-Transformers embeddings + cross-encoder reranking.

# ─── Utilities ───
httpx==0.28.1
tenacity==9.0.0
structlog==24.4.0
reportlab==4.2.5

# ─── Dev / test ───
pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.4
mypy==1.14.0
